/**
 * @property {string} title - the display name of the column.
 * @property {string} dataKey - the key corresponding to this columns in the data objects.
 */

export interface TableConfig {
  title: string;
  dataKey: string;
}
